apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: devops-service
spec:
  hosts:
  - "*"
  gateways:
  - devopsgateway
  http:
  - match:
    - uri:
      port: http
    route:
    - destination:
      host: devops
      port:
        number: 80

